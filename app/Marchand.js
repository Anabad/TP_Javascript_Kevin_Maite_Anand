'use strict'

const NBINGREDIENTS = 20;

function getRandom(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}

class Marchand{

    contructor(){
        Horaire(500,1400);
    }



}

